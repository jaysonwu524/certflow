# 安全策略

## 支持的版本

我们会为以下版本提供安全更新：

| 版本 | 支持状态 |
| --- | --- |
| main (开发版) | :white_check_mark: |
| 最新发布版 | :white_check_mark: |
| 旧版本 | :x: |

## 报告安全漏洞

**请不要通过公开 Issue 报告安全漏洞。**

如果你发现了安全漏洞，请通过以下方式负责任地披露：

### 报告方式

1. **推荐方式**：发送邮件至 security@yourdomain.com（请替换为实际邮箱）
2. 或通过 GitHub Security Advisories 私密报告

### 报告应包含的信息

为了帮助我们更好地理解和解决问题，请在报告中包含：

- 漏洞类型（如注入、XSS、权限提升等）
- 受影响的源文件路径和位置
- 特殊配置要求（如果需要）
- 复现步骤的详细说明
- 概念验证或利用代码（如果可能）
- 漏洞的潜在影响
- 你认为可能的修复建议（可选）

### 我们的承诺

- 在 **48 小时内**确认收到你的报告
- 在 **7 天内**提供详细的响应，包括下一步计划
- 在整个修复过程中保持沟通
- 在问题修复后公开致谢（如果你希望）

### 披露政策

- 我们会在收到报告后尽快确认和评估
- 我们会在合理时间内开发和测试修复方案
- 我们会发布包含修复的新版本
- 我们会在修复发布后协调公开披露

请在我们发布修复之前，**不要公开披露**该漏洞。

## 安全最佳实践

在使用 CertFlow 时，请遵循以下安全最佳实践：

### 密钥管理

- **绝不**将 `CERTFLOW_ENCRYPTION_KEY` 提交到版本控制
- 使用强随机密钥（32 字节，Base64 编码）
- 在生产环境使用 KMS 或密钥管理服务
- 定期轮换云凭证

### 访问控制

- 为阿里云 RAM 用户/角色配置最小权限
- 限制数据库访问仅限应用服务器
- 使用强密码和多因素认证
- 定期审查访问日志

### 网络安全

- 在生产环境启用 TLS/HTTPS
- 使用防火墙限制入站连接
- 部署在私有网络中
- 使用 VPN 或堡垒机访问管理界面

### 部署安全

- 不要将 `.env` 文件部署到生产环境
- 使用环境变量或密钥管理服务注入敏感配置
- 定期更新依赖包
- 启用自动安全更新

### 监控和审计

- 定期检查审计日志
- 监控异常活动和失败登录
- 设置告警规则
- 备份关键数据

## 已知的安全考虑

### 加密存储

CertFlow 使用 AES-256-GCM 加密存储所有敏感数据，包括：
- ACME 账户私钥
- 证书私钥
- 云凭证（AccessKey、Secret）
- Webhook 密钥

主加密密钥通过 `CERTFLOW_ENCRYPTION_KEY` 环境变量提供。

### 凭证验证

在创建 DNS 账户和部署目标时，系统会验证凭证的有效性和权限。但这不能替代 RAM 最小权限原则的配置。

### 测试环境

建议使用 Let's Encrypt staging 环境进行测试，避免：
- 触发速率限制
- 在生产 CA 上创建测试证书
- 误操作影响生产域名

## 安全更新通知

我们会通过以下方式发布安全更新：

- GitHub Security Advisories
- Release Notes
- 项目 README

建议 star 本项目以接收更新通知。

## 第三方依赖

CertFlow 依赖多个第三方库。我们会：

- 定期扫描依赖漏洞
- 及时更新有安全问题的依赖
- 在 Release Notes 中说明安全相关更新

用户也应定期更新到最新版本以获得安全修复。

## 致谢

我们感谢负责任披露安全问题的安全研究人员。如果你希望，我们会在以下位置公开致谢：

- SECURITY.md 中的安全研究人员名单
- Release Notes
- 项目文档

---

**感谢你帮助保持 CertFlow 的安全！**
